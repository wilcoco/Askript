# Railway 배포 설정. Dockerfile 로 빌드하고 헬스체크로 기동을 확인한다.
# 깃에 푸시하면 Railway 가 이 설정으로 자동 빌드/배포한다.

[build]
builder = "dockerfile"

[deploy]
healthcheckPath = "/healthz"
healthcheckTimeout = 120
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
